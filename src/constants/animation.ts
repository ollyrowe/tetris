// Length of the trail animation in milliseconds
export const trailAnimationLength = 1000;

// Length of the line clear animation in milliseconds
export const lineClearAnimationLength = 300;

// Delay before resuming the game after a line clear in milliseconds
export const lineClearResumeDelay = 100;
